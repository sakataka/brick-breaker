{
  "name": "brick-breaker-local",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx vite",
    "build": "bunx vite build",
    "preview": "bunx vite preview",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p tsconfig.app.json && bunx tsc --noEmit -p tsconfig.test.json",
    "lint": "bunx biome check .",
    "format": "bunx biome format --write .",
    "check": "bun run typecheck && bun run lint && bun run build"
  },
  "dependencies": {
    "ts-pattern": "^5.8.0",
    "xstate": "^5.23.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
